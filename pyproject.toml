[project]
name = "pdf-merger"
version = "0.1.0"
description = "A simple PDF merger tool"
requires-python = ">=3.10"
dependencies = [
    "pypdf>=4.0.0",
    "reportlab>=4.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[project.scripts]
pdf-merger = "pdf_merger:main"
